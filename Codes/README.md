Step1: Create the database, scripts are stored in /Build_DB. The usage is in the README of that dir
Step2: Extract the database, scripts are stored in /Extract_reads. The usage is in the README of that dir
